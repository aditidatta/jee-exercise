package project2;

import java.util.Scanner;

public class Test {
	//static StackArray<Website> stack = new StackArray<>(50);
	//static QueueArray<Website> queue = new QueueArray<>(50);
	public static Scanner sc = new Scanner(System.in);
	/*public static void main(String[] args) {
		// TODO Auto-generated method stub
		int size = 8;
		
		for(int i = 0; i < size; i++){
			System.out.println("Enter website name: ");
			Website w = new Website(sc.nextLine());
			stack.push(w);
			queue.enqueue(w);
		}
		
		
		for(int i = 0; i <= stack.top; i++){
			System.out.println(stack.element(i).getName() + "\thas been visited " + stack.element(i).getCount()+ " times");
		}
		
		System.out.println(stack.top+", "+ queue.size);
		int length = stack.top+1;
		for(int i = 0 ; i< length; i++){
			System.out.println(stack.pop().getName());
		}
		length = queue.size;
		for(int i = 0; i < length; i++){
			System.out.println(queue.peekFront().getName());
			queue.dequeue();
		}
		
		if(stack.isEmpty()){
			System.out.println("Stack OK");
		}
		
		if(queue.isEmpty()){
			System.out.println("Queue OK");
		}
		
	}*/

}
